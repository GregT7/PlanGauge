FLASK_ROUTE=http://localhost:5000
REACT_ROUTE=http://localhost:5173
DOCKER_ROUTE=http://localhost:8080
ALLOWED_ORIGINS=http://localhost:5173

SESSION_COOKIE_NAME=pg_session
SESSION_TTL_HOURS=24
COOKIE_SECURE=false        # set true in prod (HTTPS)
COOKIE_SAMESITE=Lax        # Lax is fine; Strict also ok if same-site